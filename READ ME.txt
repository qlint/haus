1. Start the laravel server (php artisan serve) and note the URL it's running on.
2. In the application, search for api_url (or just go to src/config/app.ts) and use the URL above
3. Back in the root where we started the laravel server, start the angular server (ng serve)